
@helper PageLinks____(System.Web.Mvc.HtmlHelper htmlHelper, JustBlog.Models.PagingInfo pagingInfo,
                                              Func<int, string> pageUrl)
{
    var result = new System.Text.StringBuilder();
    for (int i = 0; i < pagingInfo.TotalPages; i++)
    {

    }
}